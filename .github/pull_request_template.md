## What and why

<!-- What changes, and what problem it solves. If a measurement drove it, give the number. -->

## Checks

- [ ] `swift test` passes
- [ ] `./build.sh && open StripGauge.app` still puts the gauge in the Control Strip
- [ ] Touch Bar screenshot attached, if this changes what is drawn (⇧⌘6)
- [ ] `Sources/StripGaugeCore` still imports no AppKit
